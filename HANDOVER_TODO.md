# resume-agent 接手待办

> 目标：先恢复项目可运行性，再固化筛选结果结构与任务可靠性，最后补齐生产化能力。
>
> 当前项目根目录：`D:\AILesson\resume-agent`

## 0. 接手前确认

- [ ] 确认当前工作区实际对应 `resume-agent`，不要误操作相邻目录。
- [ ] 确认 Git 仓库、远程地址、默认分支和当前未提交修改。
- [ ] 阅读 `README.md`、`AGENTS.md`、`.env.example` 和部署配置。
- [ ] 记录当前 Python、依赖、数据库和模型服务版本。
- [ ] 备份现有数据库、日志和评测结果。

## P0：先恢复可运行性

### 配置与启动

- [ ] 统一服务端口：当前代码、Dockerfile、前端和 README 对 8000/9000 的约定不一致。
- [ ] 清理 `.env.example` 中的真实凭据样式内容，只保留占位符。
- [ ] 明确 `DEEPSEEK_API_KEY`、`MYSQL_URL`、`GITHUB_TOKEN` 的加载方式。
- [ ] 确认 SQLite 默认路径和 MySQL 连接方式均可用。
- [ ] 修复 `logs/screener.log` 不可写问题，保证本地和容器都能启动。
- [ ] 补充正式的 `docker-compose.yml`，或从 README 中删除过期描述。
- [ ] 增加启动检查：缺少模型 Key、数据库不可连接、PDF 工具不可用时给出明确错误。

### 最小验证

- [ ] 启动 FastAPI 服务。
- [ ] 验证 `GET /health`。
- [ ] 验证文本筛选：提交 JD 和简历，轮询至 `done`。
- [ ] 验证 PDF 筛选：确认 `pypdf` 解析成功，失败时 MinerU 降级路径可诊断。
- [ ] 验证数据库能创建岗位和候选人表。
- [ ] 运行 `tests/test_all.py`，记录失败项和真实通过率。

## P1：修复核心业务链路

### Supervisor 与 Agent

- [ ] 去掉 `supervisor_graph.py` 中的全局 `_jd`、`_resume`，改用 LangGraph State 或显式工具参数传递上下文。
- [ ] 将 Parser Agent 的结构化 JSON 写入共享状态。
- [ ] 让 Skill、Experience、Risk Agent 使用结构化字段，而不是默认重复读取原始简历。
- [ ] 明确四个 Agent 的调用顺序、是否允许跳过以及交叉验证条件。
- [ ] 为 Supervisor 输出建立严格 Schema：`conclusion`、`reasoning`、`score`、`details`、`activated_workers`。
- [ ] 禁止使用“从最后一个大括号截 JSON”的脆弱解析方式，改用结构化输出或稳健解析器。
- [ ] 统一结论枚举值：`建议面试`、`待定`、`不匹配`，避免中英文和旧标签混用。
- [ ] 明确 LLM 超时、重试、限流、降级和失败状态。
- [ ] 评估简历截断策略：当前截断可能导致后半段经历、项目和教育信息丢失。

### 结果与评分

- [ ] 设计综合评分公式，并真正写入 `Candidate.score`。
- [ ] 将各 Agent 原始结果按对象保存，而不是把最终回复作为单个字符串写入 `details`。
- [ ] 确保前端展示结构与后端返回结构一致。
- [ ] 为每次筛选记录模型、耗时、提示词版本和 Worker 调用情况。
- [ ] 对 LLM 返回的事实证据进行保留，方便人工复核。

## P1：修复 API 与数据持久化

- [ ] 实现前端已调用但后端缺失的 `GET /api/history`，或删除历史记录页面。
- [ ] 统一文本筛选和 PDF 筛选的持久化行为。
- [ ] 为文本筛选补充候选人姓名、邮箱、电话、性别、岗位等元数据输入。
- [ ] 修复 PDF 上传文件名未清洗的问题，避免路径和特殊字符风险。
- [ ] 为任务状态增加数据库持久化，避免服务重启后任务全部丢失。
- [ ] 为任务增加过期清理机制，避免 `_task_status` 无限增长。
- [ ] 对分页参数、状态值、岗位 ID 做合法性校验。
- [ ] 为候选人状态更新增加合法枚举和操作日志。
- [ ] 为数据库增加迁移方案，不再只依赖 `create_all()`。

## P1：前端修复

- [ ] 统一 `API_BASE` 与后端实际监听地址，支持通过环境变量或配置修改。
- [ ] 修复历史记录页面的接口依赖和错误提示。
- [ ] 修复 Agent 详情渲染：前端当前按 `details.skill/experience/risk` 读取，后端需要返回对应对象。
- [ ] 展示综合评分、PDF 解析来源、处理耗时和失败原因。
- [ ] 增加候选人基本信息输入或从简历解析结果自动填充并允许人工修正。
- [ ] 对候选人姓名、理由、简历内容等动态 HTML 做统一转义。
- [ ] 增加请求取消、轮询超时、网络重试和重复提交保护。
- [ ] 检查中文编码、状态标签、按钮文本和移动端布局。

## P2：补齐未接入能力

- [ ] 决定是否接入 GitHub Enrich Skill。
- [ ] 如果接入，新增 GitHub Agent，并明确是否影响最终评分。
- [ ] 增加 GitHub API 超时、缓存、限流和失败降级。
- [ ] 明确 MinerU 是否作为正式依赖；若不是，完善 PyPDF-only 路径。
- [ ] 评估是否需要 OCR，以支持扫描版 PDF。

## P2：测试与评测

### 单元测试

- [ ] 覆盖 Skill Matcher：命中、缺失、大小写、中文技术词和空输入。
- [ ] 覆盖 Risk Checker：每条规则、边界日期、误报和空窗期例外情况。
- [ ] 覆盖 PDF Parser：正常 PDF、空 PDF、损坏 PDF、MinerU 失败。
- [ ] 覆盖结果 Schema：缺字段、Markdown 包裹 JSON、非法结论和 LLM 超时。

### 集成测试

- [ ] 使用 Mock LLM 测试 Parser、Skill、Experience、Risk Agent。
- [ ] 使用 FastAPI TestClient 测试所有 API。
- [ ] 测试两个并发筛选请求，确认不会互相覆盖 JD 或简历。
- [ ] 测试服务重启后的任务查询和历史查询。
- [ ] 测试 SQLite 和 MySQL 两种数据库配置。

### Harness

- [ ] 固定评测数据集版本和标签版本。
- [ ] 区分“结论正确率”和“报告结构完整性”，避免结构分掩盖结论错误。
- [ ] 增加混合语言、长简历、非技术简历和真实 PDF 案例。
- [ ] 保存每次评测的模型、配置、耗时和完整结果。
- [ ] 为评测设置基线阈值，接入 CI 回归检查。

## P2：安全与生产化

- [ ] 增加认证与权限控制，至少区分招聘人员、管理员和只读用户。
- [ ] 收紧 CORS，避免长期使用 `allow_origins=["*"]`。
- [ ] 对上传文件限制大小、类型、页数和解析耗时。
- [ ] 对简历中的个人信息进行访问控制、脱敏和删除机制设计。
- [ ] 增加结构化日志、请求 ID、错误码和告警。
- [ ] 将 FastAPI `BackgroundTasks` 替换为可恢复的任务队列或工作进程。
- [ ] 增加数据库备份、恢复和数据保留策略。
- [ ] 为模型调用增加成本、Token、耗时和失败率监控。

## 推荐实施顺序

1. 配置、日志、端口和启动验证。
2. API/前端不一致问题：历史接口、结果结构、候选人持久化。
3. 去除全局变量，重构 Supervisor State 和结构化输出。
4. 补齐并发、异常、超时和任务持久化。
5. 完善单元测试、API 测试和 Mock LLM 测试。
6. 再决定 GitHub、MinerU、OCR 和生产级任务队列的范围。

## 接手完成标准

- [ ] 新成员按 README 可以在本地启动服务。
- [ ] 文本和 PDF 两种筛选都能返回结构化结果并保存记录。
- [ ] 并发提交不会串数据。
- [ ] 历史记录、候选人库和统计页面可用。
- [ ] LLM 不可用时能返回可识别的失败状态，而不是伪造默认评分。
- [ ] 测试和 Harness 有稳定、可复现的基线结果。
- [ ] 敏感信息不进入代码库，简历数据有明确的访问和删除策略。
